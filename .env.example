# ─────────────── JWS 鉴权（必填） ───────────────
APP_ID=your_app_id_here
PRIVATE_KEY_FILE=./private_key.pem
ORG_ID=37695
LIBRARY_ID=smh2yeaqwkt6nznb
PHONE_NUMBER=18301413027
COUNTRY_CODE=+86
TOKEN_TTL_SECONDS=300

# ─────────────── 工作目录（必填） ───────────────
# 分类器扫描的根目录，也是上传时将 CSV 相对路径解析为绝对路径的基准
WORKSPACE_PATH=/Users/admi_rocio/Desktop/auto_upload/workspace

# ─────────────── 分类器配置（可选，默认在 WORKSPACE_PATH 父目录） ───────────────
# TASK_CONFIG_PATH=/path/to/task_classification.json
# TEAM_LIST_PATH=/path/to/current_teamlist.json
# OUTPUT_CSV_PATH=/path/to/分类结果.csv

# ─────────────── 上传策略（可选） ───────────────
MAX_TEAM_CONCURRENT=3
MAX_FILE_CONCURRENT=5
CONFLICT_STRATEGY=overwrite
AUTO_ADJUST_CONCURRENT=true
