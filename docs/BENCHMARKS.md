# Benchmarks

> Phase 7 之后填充。每组实验包含：场景描述、参数、结果数据、结论。
> 参见 BLUEPRINT § 十一 的实验列表。

## 待做实验
- 重复文件秒传命中率（SMH 三段协商）
- 平台层 dedup 价值（HQ → N 子公司）
- 流式 vs 整文件入内存
- multipart vs 单段
- AIMD vs 固定限流
- 单 worker vs N worker 横向扩缩
- 优雅关停 vs 强杀
