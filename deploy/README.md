# Deploy

docker-compose、可观测组件配置、SQL 初始化脚本。

## 当前状态
**Phase 0 占位**。

## 计划填充
- `docker-compose.yml` — Phase 3 起，包含 PG / Redis / Kafka / MinIO
- `docker-compose.obs.yml` — Phase 5 起，包含 Jaeger / Prometheus / Grafana / Loki
- `prometheus/prometheus.yml`
- `otel/otel-config.yaml`
- `grafana/dashboards/`
